apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: scratch-prod
  namespace: default
spec:
  project: default

  source:
    repoURL: https://github.com/CanonicalFlow/infrastructure
    targetRevision: main
    path: apps/scratch/prod

  destination:
    server: https://kubernetes.default.svc
    namespace: prod

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
